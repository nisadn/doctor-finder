export type DoctorType= {
    name: string
    imgUrl: string
    about: string
    specialization: string
    hospital: string
    price: string
}

export type Option = {
    id: string
    name: string
}